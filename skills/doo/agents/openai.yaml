interface:
  display_name: "Doo"
  short_description: "Run tasks through evidence-backed verification"
  default_prompt: "Use $doo to carry this repository task through implementation and evidence-backed verification."
policy:
  allow_implicit_invocation: true
